Y = zeros(12, 1);
t = 0:0.01:20; 
%options = odeset('OutputFcn', @odeplot);
[Mt, My] = ode45(@hexa, t, Y);%solve ode and get mt, my

%plot mt, my
figure(1);
plot (Mt, My(:,1) );
FIGURE(
plot (Mt, My(:,12) );